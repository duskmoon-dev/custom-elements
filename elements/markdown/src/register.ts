/**
 * Auto-register el-dm-markdown custom element
 *
 * @example
 * ```ts
 * import '@duskmoon-dev/el-markdown/register';
 *
 * // Now you can use <el-dm-markdown> in HTML
 * ```
 */
import { register } from './index.js';

register();
